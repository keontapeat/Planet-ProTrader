──────────────────────────────────────────────────────────────────────────────
STEP-5 STILL CRASHES → INSTALL THE OLDER VC++ 2015 RUNTIME
──────────────────────────────────────────────────────────────────────────────
Reason
• Your winetricks build is old (20210206) – it has **vcrun2015** but not
  vcrun2019 / 2022.  So the verb fails and the DLL never installs.

Fix – run **exactly** the six “$” lines below
────────────────────────────────────────────
$ export WINEPREFIX=$HOME/.wine

# (1) set Windows version to win10 (required for ucrt DLLs)
$ winetricks -q win10

# (2) install VC++ 2015 runtime (includes the Universal CRT we need)
$ winetricks -q vcrun2015

# (3) verify it’s really there
$ winetricks list-installed | grep vcrun     # expect: vcrun2015

# (4) flush caches & test again
$ wineserver -k && sleep 2
$ DISPLAY=:1 wine C:\\opt\\goldex-bot\\winevenv\\Scripts\\python.exe - <<'PY'
import MetaTrader5 as mt5, os, sys; from dotenv import load_dotenv
load_dotenv('/opt/goldex-bot/.env')
ok = mt5.initialize(login=int(os.getenv('DEMO_LOGIN',0)),
                    password=os.getenv('DEMO_PASSWORD',''),
                    server=os.getenv('DEMO_SERVER',''))
print("✅ Connected — Balance:", mt5.account_info().balance) if ok else \
      sys.exit(f"❌ init failed: {mt5.last_error()}")
mt5.shutdown()
PY

If you finally see the green ✅ line, launch the bot:

$ tmux new -s goldex -d "export DISPLAY=:1 && wine C:\\opt\\goldex-bot\\winevenv\\Scripts\\python.exe C:\\opt\\goldex-bot\\goldex_bot.py"
$ echo '🚀  GoldexBot is now running inside tmux session "goldex".'

Watching logs:
$ tmux attach -t goldex   (Ctrl-b then d to detach)

Still crashes?
──────────────
1. Make sure `winetricks list-installed` shows vcrun2015.  
2. If it does and `fetestexcept` persists, upgrade Wine to 8.x (one line):
   $ apt install -y --install-recommends winehq-stable
   Then rerun the test script.

One clean vcrun2015 install **should** resolve fetestexcept on Wine ≥5.  
Paste the next full error if it doesn’t.  We’re close!
──────────────────────────────────────────────────────────────────────────────