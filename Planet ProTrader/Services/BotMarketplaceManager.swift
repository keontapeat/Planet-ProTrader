import SwiftUI
import Foundation
import Combine

// MARK: - DEPRECATED - This service conflicts with BotStoreService
// Remove this file completely or rename to avoid conflicts
// All marketplace functionality should use BotStoreService.shared

// If you need this service for a different feature, rename it to avoid conflicts
// For example: "BotHiringManager" or "PersonalBotManager"

/*
@MainActor
class BotMarketplaceManager: ObservableObject {
    // This service has been deprecated in favor of BotStoreService
    // to avoid model conflicts and maintain architectural consistency
    
    // If you need this functionality, please:
    // 1. Rename this class to avoid conflicts
    // 2. Use compatible model properties
    // 3. Ensure proper integration with existing views
}
*/